<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.odiousPanda.rainbowKt">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/logo_rainbow"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_rainbow"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <receiver android:name="com.odiousPanda.rainbowKt.widgets.NormalWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="actionUpdate" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/normal_widget_info" />
        </receiver>

        <activity android:name="com.odiousPanda.rainbowKt.activities.MainActivity"
            android:configChanges="locale|orientation|uiMode"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".utilities.DailyNotificationAlarmReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="rainbow.notification.notify" />
            </intent-filter>
        </receiver>
    </application>

</manifest>